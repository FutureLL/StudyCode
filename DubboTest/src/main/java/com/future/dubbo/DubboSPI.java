package com.future.dubbo;import com.future.dubbo.service.Hello;import org.apache.dubbo.common.extension.ExtensionLoader;/** * @Description: * @Author: lilei58 * @Date: Created in 2022/1/19 上午8:51 * * 1. 接口增加注解: import org.apache.dubbo.common.extension.SPI; * 2. 创建 META-INF.dubbo 目录,目录下创建接口路径名的文件,同时在文件中写入如下数据: *      helloImpl = com.future.dubbo.service.impl.HelloImpl *      hello2Impl = com.future.dubbo.service.impl.Hello2Impl */public class DubboSPI {    public static void main(String[] args) {        // 使用的话需要在接口中增加一个注解 @SPI        ExtensionLoader<Hello> extensionLoader = ExtensionLoader.getExtensionLoader(Hello.class);        // TODO 这里拿到的是 HelloWrapper 类        Hello helloImpl = extensionLoader.getExtension("helloImpl");        helloImpl.sayHello();        // 自动注入        // AOP    }}